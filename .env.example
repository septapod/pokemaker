# PokéMaker Environment Variables
# Copy this file to .env and fill in your actual values
# SECURITY: Never commit .env file with real API keys to git

# Supabase Configuration (CLIENT-SIDE - Safe to expose)
# Get these from your Supabase project settings: https://app.supabase.com
# The anon key is designed to be public and protected by Row Level Security (RLS)
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# OpenAI Configuration (SERVER-SIDE ONLY)
# Get your API key from: https://platform.openai.com/api-keys
# IMPORTANT: This key should NEVER have VITE_ prefix to prevent browser exposure
# For local development: Add to .env (not committed to git)
# For Vercel deployment: Add via Settings → Environment Variables
OPENAI_API_KEY=your_openai_api_key_here
